# PixelSnack 手动验收清单

请使用真实鼠标、触控设备和下载文件完成以下检查。每项记录“通过 / 失败 + 设备与浏览器 + 复现步骤”。

## 本轮修复重点

- 在 768、900、1080 像素高的窗口分别打开转图、新建和导出弹窗：确认内容区域能滚动，标题和底部主按钮始终可达，不会被视口裁掉。
- 转图弹窗上传图片并生成预览后，分别滚动左侧预览区和右侧设置区，确认两侧互不抢滚动；手机端应为一条自然的纵向滚动。
- 桌面端确认“新建 / 导入 / 备份”位于导航器顶部，“清空 / 撤销 / 重做”位于右侧色板顶部，主工具条位于画布下方且不遮住内容。
- 桌面端确认左上角没有类似返回的箭头按钮，且“图片转拼豆”只在顶部出现一次；手机端仍从底栏“转图”进入。
- 拖动左侧导航器的竖向缩放滑杆，确认画布随滑杆连续放大、缩小，百分比同步变化，并且视口中心基本保持不跳动。
- 上传普通 JPG/PNG，拖动画面、调整裁切缩放后点击“生成拼豆预览”，确认不再出现 Worker、跨域或 Script cannot be accessed 错误。
- 分别选择“最近邻 / 平滑缩放 / 高质量平滑”，用像素画、插画和照片各转换一次，确认边缘效果有可见差异且结果稳定。
- 默认选择“互动描摹”：创建后画布应为空，转换图以浅色底图显示；用吸管取底图色、铅笔描摹，并用填充工具完成一个同色连通区域。
- 选择“直接生成”后创建，确认画布直接得到完整像素图；PNG/PDF 只导出真正填入的豆，不把未完成的浅色底图算作成品。
- 检查顶部、左右面板、弹窗和表单：浅色背景均使用清晰的深色文字；深色色块上的色号仍能辨认。

## 桌面编辑

- 新建 48×48 作品，快速斜向拖动画笔，确认线条连续、不漏格；一次撤销应删除整条笔画。
- 分别测试橡皮、区域填充、吸管、画布平移、滚轮指针中心缩放。
- 将笔刷调为 8×8，在画布四角绘制，确认不会越界或卡顿。
- 创建 256×256 作品，连续绘制 2 分钟，确认输入跟手、页面不冻结。
- 使用 B/E/G/I/H、Ctrl+Z、Ctrl+Shift+Z、Ctrl+S，确认快捷键与输入框不冲突。

## 手机和平板

- 竖屏与横屏分别确认画布、底部工具、色板、转图、导出均可访问。
- 单指绘制；双指缩放和平移时不应误落豆；松开后可以继续绘制。
- 使用触控笔连续绘制，再用手指双指缩放，确认两类输入不互相锁死。
- 检查刘海屏安全区域及浏览器地址栏收起/展开时，底部工具栏不被遮挡。

## 保存与文件

- 绘制后等待“已保存”，刷新页面、关闭重开浏览器，确认作品恢复。
- 导出 `.pixelsnack` 后重新导入，确认尺寸、色板、所有格子和统计一致。
- 导入损坏或改名的文件，确认只显示错误提示且当前作品不被覆盖。
- 导入包含 `brand,code,name,hex` 的 CSV/JSON 色板，确认色号和颜色正确。

## 图片转换与导出

- 在“裁切填满”模式拖动画面、调整图片缩放，确认最终预览与裁切框构图一致。
- 切换“完整保留”，确认整张图片可见，透明或留白边缘不会被错误填色。
- 导入照片和透明 PNG，各测试一次开启/关闭抖动；确认透明区域保持为空格。
- 确认转图会创建新作品，不覆盖原作品。
- 分别导出普通 PNG、带网格 PNG、透明 PNG，检查像素边缘没有模糊。
- 打印 PDF 时选择 100% 比例，测量 100 mm 校准线；检查分页坐标、色号和数量。

## PWA 与兼容性

- 在 Chrome/Edge 安装为应用，断网重启后确认编辑、转图和导出仍可使用。
- 至少在 Chrome、Edge、Safari 各完成一次“绘制 → 刷新恢复 → PNG 导出”。
- 若出现问题，请附浏览器版本、设备型号、项目尺寸和复现前最后三步操作。
