# PixelSnack 手动验收清单

请使用实际鼠标、触控设备和下载文件完成以下检查。每项记录“通过 / 失败 + 设备与浏览器 + 复现步骤”。

## 桌面编辑

- 新建 48×48 作品，快速斜向拖动画笔，确认线条连续、不漏格；一次撤销应删除整条笔画。
- 分别测试橡皮、封闭区域填充、吸管、画布平移、滚轮指针中心缩放。
- 将笔刷调为 8×8，在画布四角绘制，确认不会越界或卡顿。
- 创建 256×256 作品，连续绘制 2 分钟，确认输入跟手、页面不冻结。
- 使用 B/E/G/I/H、Ctrl+Z、Ctrl+Shift+Z、Ctrl+S，确认快捷键与输入框不冲突。

## 手机和平板

- 竖屏与横屏分别确认画布、底部工具、色板、转图、导出均可访问。
- 单指绘制；双指缩放和平移时不应误落豆；松开后可以继续绘制。
- 使用触控笔连续绘制，再用手指双指缩放，确认两类输入不会互相锁死。
- 检查刘海屏安全区域及浏览器地址栏收起/展开时，底部工具栏不被遮挡。

## 保存与文件

- 绘制后等待“已保存”，刷新页面、关闭重开浏览器，确认作品恢复。
- 导出 `.pixelsnack` 后重新导入，确认尺寸、色板、所有格子和统计一致。
- 导入损坏或改名的文件，确认只显示错误提示且当前作品不被覆盖。
- 导入包含 `brand,code,name,hex` 的 CSV/JSON 色板，确认色号和颜色正确。

## 图片转换与导出

- 导入照片和透明 PNG，各测试一次开启/关闭抖动，并在处理中尝试关闭弹窗。
- 确认转图会创建新作品，不覆盖原作品；透明区域保持为空格。
- 分别导出普通 PNG、带网格 PNG、透明 PNG，检查像素边缘没有模糊。
- 打印 PDF 时选择 100% 比例，测量 100 mm 校准线；检查分页坐标、色号和数量。

## PWA 与兼容性

- 在 Chrome/Edge 安装为应用，断网重启后确认编辑、转图和导出仍可使用。
- 至少在 Chrome、Edge、Safari 各完成一次“绘制 → 刷新恢复 → PNG 导出”。
- 若出现问题，请附浏览器版本、设备型号、项目尺寸和复现前最后三步操作。
